package ui.play;

import javax.swing.*;
import java.awt.*;

public class GameMenu extends JPanel {

    public GameMenu() {
        setSize(1000, 1000);
        setFocusable(true);
        setBackground(Color.BLACK);
    }
}
